class Solution:
    def twoSum(self, arr, target):
        s = set()
        
        for num in arr:
            if (target - num) in s:
                return True
            s.add(num)
            
        return False
